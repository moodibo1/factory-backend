DATABASE_URL=postgresql://postgres:password@localhost:5432/factory_issues
SECRET_KEY=supersecretkey123changeme
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Supabase — backend uses these to send verification emails via service role
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase — frontend uses these (set in Vercel env vars, not here)
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
